<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SafetyApp365" parent="Theme.MaterialComponents.DayNight">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/orange</item>
        <item name="colorPrimaryDark">@color/rose</item>
        <item name="colorAccent">@color/white</item>
    </style>

    <style name="Theme.SafetyApp365.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimaryDark">@color/candy</item>
    </style>

    <style name="Theme.SafetyApp365.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.SafetyApp365.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>